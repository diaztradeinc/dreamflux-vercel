
// Lightweight Sortable mock (replace with full Sortable.js if needed)
window.Sortable = {
  create: function (el, opts) {
    console.log("Sortable enabled on:", el);
    // Mock drag behavior (to be replaced with full lib in real use)
  }
};
